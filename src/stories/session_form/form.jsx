import React from 'react';
import Form from '../../packages/session_form/form';

// buttonText has to be 'sign in' or 'sign up'

const TestForm = () => (
    <Form
        buttonText="sign up">
    </Form>
)

export default TestForm;